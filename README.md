# DeckLib
A module for simple 52 card deck logic using JS.

Contains the following classes:
Card, Deck, DiscardPile, Hand, Shuffler.


## Installation 

```bash
npm install github:eb225nm/DeckLib

Place import { Deck, Hand, DiscardPile, Card } from 'DeckLib'; at the top of your file to use the libraries features.